services:
  s3admin:
    image: dpage/pgadmin4
    container_name: 68-s3-admin
    environment: 
      ADMIN_EMAIL: ${ADMIN_EMAIL}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
    env_file:
      - .env
    ports: 
      - "${ADMIN_PORT}:80"